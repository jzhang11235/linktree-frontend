import styled from 'styled-components';

const ExpandableLinkContainer = styled.div`
  border-radius: 4px;
  background: #f5f7f8;
  color: ${props => props.theme.colors.text};
`;

export default ExpandableLinkContainer;
